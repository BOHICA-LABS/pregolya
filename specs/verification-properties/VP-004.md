---
document_type: verification-property
level: L4
vp_id: VP-004
title: MCP ToolException Type-Identity Preservation
status: draft
producer: architect
timestamp: 2026-07-14T14:00:00Z
phase: 1b
inputs:
  - .factory/specs/behavioral-contracts/ss-09/BC-2.09.004.md
input-hash: "c8f0fc6"
traces_to: ARCH-INDEX.md
source_bc: BC-2.09.004
bc_anchor: BC-2.09.004
di_anchor: DI-014
module: mcp::adapter
crate: ferrochain-mcp
tool: integration
proof_method: manual
proof_phase: 3
priority: P1
red_gate: true
red_gate_source: R11
feasibility: feasible
verification_lock: false
proof_completed_date: null
proof_file_hash: null
lifecycle_status: active
introduced: v1.0.0-greenfield
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
withdrawn: null
withdrawal_reason: null
removed: null
removal_reason: null
version: "1.4"
changelog:
  - "1.4 (FIX-BURST-280-corr/F-P175-A10-rework/2026-07-28): Rework paper-fix from v1.3. v1.3 replaced `is_some()` with a `format!(\"{source:?}\").starts_with(\"ToolExecution\")` debug-string match — still a paper-fix: passes for any type whose Debug representation starts with `ToolExecution`, including unrelated types and bare string errors; coupled to `#[derive(Debug)]` formatting; fails TD-VSDD-059 test. Correct fix: typed downcast to `McpError` via `source.downcast_ref::<McpError>()` (available on `dyn Error + 'static` via the `+ 'static` bound); `matches!` assertion on `McpError::ToolExecution` variant. Deleted incorrect comment claiming the `+ 'static` downcast facility was absent; deleted conditional hedge `if McpError: Any + 'static`. `FerrochainError::source()` is `Option<&(dyn Error + 'static)>`; `downcast_ref::<McpError>()` is directly available. TD-VSDD-059 paper-fix closure."
  - "1.3 (FIX-BURST-280/F-P175-A10/2026-07-28): Replace `err.source().is_some()` false-green assertion with downcast type-identity check. `is_some()` passes when any error is in the source chain; BC-2.09.004 TV-005 requires specifically `McpError::ToolExecution`. New assertion verifies the concrete variant via debug representation. TD-VSDD-059 false-green class closure."
  - "1.2 (FIX-BURST-256/F-P155-04-sweep/2026-07-24): Sync §BC Traceability Title cell to canonical BC H1: BC-2.09.004 'MCP bare ToolException re-raise preserving type identity (Red Gate — R11)' → 'MCP Bare ToolException Re-Raise Preserving Type Identity (Red Gate — R11)' (case normalization)."
  - "1.1 (F-P124-02, 2026-07-19): Upgrade level L3→L4 per L4-verification-property-template.md canonical template (rc.22); add extended frontmatter (inputs, source_bc, proof_method, feasibility, verification_lock, lifecycle fields, version, changelog); add Source Contract, Proof Method, Lifecycle body sections; rename Verification Strategy to Proof Harness Skeleton per template heading convention."
  - "1.0 (2026-07-14, initial): base VP authored."
---

# VP-004: MCP ToolException Type-Identity Preservation

## Property Statement

When an MCP server raises a bare `ToolException`, the ferrochain MCP adapter re-raises it
as an `MCP ToolException` error preserving exact type identity. The error type is not
downcast, wrapped in a generic error, or silently converted to a string error.

**Invariant (DI-014):** MCP tool errors propagate as `Err(FerrochainError { code: "E-MCP-001", .. })`
with the original exception type information preserved in the error source chain.

## Source Contract

- **BC:** BC-2.09.004 — MCP Bare ToolException Re-Raise Preserving Type Identity (Red Gate — R11)
- **Postcondition/Invariant:** Postcondition 1 — the caller observes `Err(FerrochainError { component: MCP, category: TOOL, code: "E-MCP-001", message: "ToolException: MCP server '<server>' raised ToolException for tool '<tool>': <message>", .. })`; Postcondition 4 — `FerrochainError::source` retains the original `McpError::ToolExecution` for downstream introspection; the `handle_tool_errors` flag does NOT suppress this path.

## BC Traceability

| BC | Title |
|----|-------|
| BC-2.09.004 | MCP Bare ToolException Re-Raise Preserving Type Identity (Red Gate — R11) |

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| Integration test | Red Gate test suite (cargo nextest) | N/A — behavioral test | Mock MCP server raising bare `ToolException`; asserts `FerrochainError { code: "E-MCP-001", category: TOOL, .. }` with original exception type accessible via `error.source()`; flag irrelevance verified (handle_tool_errors=true and false both produce same result) |

## Proof Harness Skeleton

**Tool:** Integration test (behavioral — not Kani-tractable; involves I/O transport)

Red Gate test: `tests/red_gate/test_BC_2_09_004_mcp_tool_exception.rs`

Test scenario: mock MCP server that raises a bare `ToolException`; assert that the
ferrochain adapter surfaces `Err(FerrochainError { code: "E-MCP-001", .. })` with
the original exception type accessible via `error.source()`.

```rust
// tests/red_gate/test_BC_2_09_004_mcp_tool_exception.rs
// Red Gate: must compile and FAIL before implementation (error swallowed or wrong code)

#[tokio::test]
async fn bc_2_09_004_bare_tool_exception_preserves_type_identity() {
    let mock_server = MockMcpServer::new_raises_tool_exception("unauthorized: tool requires auth");
    let client = McpTool::connect(mock_server.config()).await.unwrap();

    let result = client.run(ToolInput::default()).await;

    assert!(result.is_err());
    let err = result.unwrap_err();
    assert_eq!(err.code(), "E-MCP-001");
    assert_eq!(err.category(), Category::Tool);
    // TYPE IDENTITY ASSERTION (BC-2.09.004 TV-005 / PC-4):
    // `err.source()` returns `Option<&(dyn Error + 'static)>`; the `+ 'static` bound
    // enables typed downcast via `downcast_ref`. A non-McpError cause — including a bare
    // String error — must FAIL this assertion. Passing for any error-with-a-source is
    // the false-green condition this test gates against.
    let source = err.source()
        .expect("E-MCP-001 must carry the original McpError in the source chain");
    let mcp_err: &McpError = source
        .downcast_ref::<McpError>()
        .expect("source must be McpError, not a different error type; non-McpError cause FAILS here per BC-2.09.004 TV-005");
    assert!(
        matches!(mcp_err, McpError::ToolExecution { .. }),
        "source must be McpError::ToolExecution variant specifically; got: {mcp_err:?}"
    );
}

#[tokio::test]
async fn bc_2_09_004_handle_tool_errors_flag_irrelevant() {
    // handle_tool_errors=false: bare ToolException path must still propagate as E-MCP-001
    let mock_server = MockMcpServer::new_raises_tool_exception("auth failure");
    let client = McpTool::connect_with_options(
        mock_server.config(),
        McpOptions { handle_tool_errors: false, ..Default::default() },
    ).await.unwrap();

    let result = client.run(ToolInput::default()).await;
    let err = result.unwrap_err();
    assert_eq!(err.code(), "E-MCP-001");
}
```

## Feasibility Assessment

**Feasibility: HIGH.** This is a behavioral test, not a formal proof. The property is
verified by constructing a mock MCP server that raises known exception types and asserting
the adapter's error handling preserves the type chain.

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | Bounded | Mock server; fixed exception types |
| Proof complexity | Low | Direct assertion on error code and source chain |
| Tool support | Supported | cargo nextest; mock MCP server via test doubles |
| Estimated proof time | < 5s | Integration test; no external services |

**Phase 6 consideration:** No Kani harness planned. Red Gate integration test is sufficient.

## Proof Obligations

- [ ] Mock MCP server raises bare ToolException (R11 upstream test void)
- [ ] `FerrochainError::source()` contains the original exception
- [ ] Error code is `E-MCP-001` (not a generic error)
- [ ] Red Gate test passes before Phase 3 story delivery for SS-09

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-07-14 | architect |
| Red Gate test authored | | test-writer |
| Red Gate test passes (implementation complete) | | implementer |
| Locked (VERIFIED) | | formal-verifier |
