---
document_type: verification-property
level: L4
vp_id: VP-013
title: "BashTool Risk Floor — ReadOnly and Low ActionRisk Always Return Err(E-TOOLS-007); Medium Is the Hard Floor"
status: draft
producer: architect
timestamp: 2026-08-16T00:00:00Z
phase: 1b
inputs:
  - .factory/specs/behavioral-contracts/ss-23/BC-2.23.005.md
  - .factory/specs/architecture/decisions/ADR-020-first-party-tool-library.md
input-hash: "95fcb05"
traces_to: ARCH-INDEX.md
source_bc: BC-2.23.005
bc_anchor: BC-2.23.005
di_anchor: DI-014
module: tools::shell
crate: pregolya-tools
tool: Kani
proof_method: kani
proof_phase: 6
priority: P1
red_gate: false
red_gate_source: null
feasibility: feasible
verification_lock: false
proof_completed_date: null
proof_file_hash: null
lifecycle_status: active
introduced: v1.0.0-greenfield
modified: []
deprecated: null
deprecated_by: null
replacement: null
retired: null
withdrawn: null
withdrawal_reason: null
removed: null
removal_reason: null
version: "1.17"
changelog:
  - "1.17 (R30/F-P2A128-01/2026-08-28): F-P2A128-01 PROCESS-GAP — Kani harness import convention corrected for separate-file proofs. Target is `tools::shell` submodule (`src/shell/bash.rs`); correct crate path is `use crate::shell::bash::*`. Fixed: removed `#[cfg(kani)] mod proofs {}` wrapper; replaced `use super::*` with `use crate::shell::bash::*`; proof functions dedented from 4-space to top-level (0-space). No property, invariant, or proof logic changed. input-hash updated: 95fcb05 (pre-existing BC input drift corrected)."
  - "1.16 (INVESTIGATE-RECONCILE/2026-08-21): Fix target file path: `pregolya-tools/src/shell.rs` → `pregolya-tools/src/shell/bash.rs`. Story S-1.22 creates BashTool in a `shell/` subdirectory (`shell/bash.rs`); `check_risk_floor` pure function lives in `bash.rs`. Module identifier `tools::shell` (frontmatter) is correct and unchanged. No property or proof harness logic changed."
  - "1.15 (BURST-308/D26-EXEC-propagation/2026-08-17): §BC Contradictions Flagged RESOLVED block: '12-category axis' → '13-category axis' per ADR-010 §Category Axis Expansion (D26). EXEC is the 13th category added by D26; the prior `Category::CONFIGURATION` label was non-canonical both before and after D26. No proof method or harness change."
  - "1.14 (FIX-BURST-291/F-P1D182-01/2026-08-16): Fix phantom §-anchors per D-134 corpus sweep. (1) §Feasibility Assessment Note: 'BC-2.23.005 §Category was set to `VAL` (burst-232)' → 'BC-2.23.005 §Postconditions (PC-4) category is `VAL` (burst-232)'. (2) §BC Contradictions Flagged RESOLVED block (lines 265/267): '§Category was corrected to' → '§Postconditions (PC-4) category corrected to'; '§Category = VAL' → '§Postconditions (PC-4) category = VAL'. (3) Same RESOLVED block line 268: 'error-taxonomy §TOOLS' → 'error-taxonomy.md §Component: TOOLS'. (4) Changelog v1.10 '→' target text updated to match new live-body wording. Rationale: BC-2.23.005 has no §Category heading; category field lives in §Postconditions PC-4 (verified). error-taxonomy.md has no §TOOLS heading; real heading is §Component: TOOLS (pregolya-tools) (verified). input-hash refreshed (BC-2.23.005.md and ADR-020 changed since last hash; pre-existing drift resolved)."
  - "1.13 (FIX-BURST-276/F-P173-504/2026-07-27): Remove stale '(Red Gate)' labels from §Source Contract and §BC Traceability. red_gate: false is the authoritative frontmatter value; TV-005 is a BC-2.23.005 unit test vector, not a Red Gate compile-and-fail test. (1) §Source Contract TV-005 line: '(Red Gate)' parenthetical removed. (2) §BC Traceability Specific anchors: '§TV-005 (Red Gate)' → '§TV-005'. Sibling sweep per TD-VSDD-060: all 13 VP bodies checked; only VP-012 (fixed in same burst) and VP-013 (this fix) carried stale (Red Gate) labels with red_gate: false frontmatter."
  - "1.12 (FIX-BURST-273/enum-variant-casing/2026-07-25): Casing residue in §BC Contradictions Flagged — `Category::VAL` → `VAL` to match v1.10 de-pin intent (category code value, not a Rust path; removes `Category::` prefix; verify-enum-variant-casing.sh PASS after fix)."
  - "1.11 (FIX-BURST-273/lifecycle-adjudication/2026-07-25): Align four sites to product-owner lifecycle adjudication: E-TOOLS-007 risk-floor rejection is at ToolConfig::override_risk CALL TIME, not registry time. (1) EC-004: 'override_risk(ReadOnly) at registry time → Err(E-TOOLS-007) — graph does not start' → 'at call time → Err(E-TOOLS-007); tool configuration fails at the override_risk call site before the registry or graph is involved'. (2) EC-005: 'override_risk(Medium) at registry time → accepted (Medium is the floor)' → 'at call time → Ok(ToolConfig); accepted because Medium is the hard floor; configuration succeeds at the call site'. (3) BC-2.05.007 Contribution: 'VP-013 floor check runs at registry time, before hook dispatch' → 'VP-013 floor check runs at configuration time (inside ToolConfig::override_risk), before any registry registration or hook dispatch'. (4) Proof Method §Harness Boundary: 'extracted from BashTool registration logic; ToolRegistry::register flow calls check_risk_floor as a validation step' → 'reached via ToolConfig::override_risk call path; floor validation is immediate and unavoidable at the call site; effectful ToolRegistry excluded from Kani scope'. Additional sweep: §Source Contract ADR anchor 'at startup' → 'at call time (inside ToolConfig::override_risk)'."
  - "1.10 (FIX-BURST-273/F-P171a-15+11/2026-07-25): F-P171a-15 — `risk_floor_exhaustive_coverage` doc-comment incorrectly said 'Requires `kani::Arbitrary` derived for ActionRisk, or a symbolic bool approach'; the harness body deliberately uses bounded-integer encoding (`u8` index) to avoid the Arbitrary derivation requirement. Remove the 'Requires' clause and replace with accurate description. Also fix §Proof Method conditional 'via `kani::any::<ActionRisk>()` if `kani::Arbitrary` is derived' → 'via explicit bounded-integer encoding (0=ReadOnly, 1=Low, 2=Medium, 3=High); no Arbitrary derivation required'. F-P171a-11 — de-pin live-body version reference in §Feasibility Assessment §Note: 'BC-2.23.005 was amended to `VAL` in burst-232 (v1.1)' → 'BC-2.23.005 §Postconditions (PC-4) category `VAL` (burst-232)'. Same instance in §BC Contradictions Flagged body: 'BC-2.23.005 was amended to `Category::VAL` in v1.1' → 'BC-2.23.005 §Postconditions (PC-4) category corrected to `VAL`'."
  - "1.9 (F-P170-16/burst-272/2026-07-25): Rename retired symbol at §Source Contract §Invariant — Risk floor clause. Two occurrences: `BashTool::set_risk(ReadOnly)` → `ToolConfig::override_risk(ActionRisk::ReadOnly)` and `BashTool::set_risk(Low)` → `ToolConfig::override_risk(ActionRisk::Low)` on a `BashTool` instance. Mirrors BC-2.23.005 §Invariants canonical phrasing per ADR-020 Decision 3."
  - "1.8 (FIX-BURST-272/DEFECT-1/2026-07-25): De-pin live-body BC version pin per TD-VSDD-091 BC-pin variant: §BC Contradictions RESOLVED notice — 'BC-2.23.005 v1.1 = VAL' → 'BC-2.23.005 §Category = VAL'. Newly-authored text (burst-232 RESOLVED notice), not grandfathered."
  - "1.7 (FIX-BURST-272/F-P170-05/2026-07-25): Purge phantom `ActionRisk::Critical` variant throughout. Canonical `ActionRisk` definition in interface-definitions.md §PreToolCallHook has 4 variants (ReadOnly/Low/Medium/High — #[non_exhaustive]); `Critical` appeared only in this VP, with no BC coverage, no E-TOOLS-* code, and no interface-definitions entry. Changes: (1) §Property Statement — remove 'or `Critical`' from positive arm; formal invariant — remove `r == Critical` disjunct; update domain to 4 variants. (2) §Proof Method — 5 variants → 4 variants; remove Critical from bounded-variant enumeration. (3) §Proof Harness Skeleton `risk_floor_accepts_at_or_above_medium` — remove `ActionRisk::Critical` arm. (4) §Proof Harness Skeleton `risk_floor_exhaustive_coverage` — `kani::assume(idx <= 4)` → `kani::assume(idx <= 3)`; `_ => ActionRisk::Critical` → `_ => ActionRisk::High`; floor invariant comment `idx < 2` boundary unchanged (ReadOnly=0, Low=1 still floor; Medium=2, High=3 pass). (5) §Feasibility Assessment — '5-variant enum' → '4-variant enum'; remove Critical from repr(u8) ordering; '0..=4' → '0..=3'. (6) §Proof Obligations — remove `ActionRisk::Critical` from constructable-values checklist."
  - "1.6 (FIX-BURST-270/P1D-168-casing/2026-07-25): PascalCase canon sweep — §Feasibility Assessment note: Category::VAL → Category::Val; §Proof Obligations: Category::VAL → Category::Val per ADR-010 v1.9 Direction B adjudication (retracts v1.5/F-P167-05 direction on this file)."
  - "1.5 (FIX-BURST-269/F-P167-04+05/2026-07-25): F-P167-04 — sync §Source Contract BC title to canonical BC-2.23.005 H1: retired 'E-TOOLS-004/005/007' enumeration → canonical 'E-TOOLS-004/007' (burst-247 title policy; §BC Traceability was already correct from burst-256 sweep; §Source Contract was the missed sibling section). F-P167-05 — replace non-canonical Category::Val (PascalCase) with Category::VAL (SCREAMING_CASE) at two sites in §Feasibility Assessment and §Proof Obligations. SCREAMING_CASE is the canonical casing per ADR-010 Category Codes list (VAL, not Val)."
  - "1.4 (FIX-BURST-268/OBS-P166-A/2026-07-25): De-pin two live-body version references per TD-VSDD-091: (1) §Feasibility Assessment 'error-taxonomy.md (v1.31, D23)' → 'error-taxonomy.md §Component: TOOLS (registered at D23)'; (2) §Proof Obligations 'error-taxonomy v1.31' → 'error-taxonomy.md §Component: TOOLS'. Changelog entries and §BC Contradictions RESOLVED note are historical records exempt from TD-VSDD-091."
  - "1.3 (FIX-BURST-256/F-P155-04-sweep/2026-07-24): Sync §BC Traceability Title cells to canonical BC H1s: BC-2.23.005 → added 'BashOutput; 256 KiB Output Cap; 30 s Timeout; E-TOOLS-004/007' middle detail; BC-2.23.001 'ReadFileTool — PathGuard-Confined File Read' → '...max_bytes 1 MiB Limit; E-TOOLS-001/002/008'; BC-2.05.007 'PreToolCallHook Dispatch — Fail-Closed Deny' → 'PreToolCallHook Dispatch — pre_invoke Contract; Approve/Deny/Edit/PendingHumanApproval; Fail-Closed Deny (VP-011 Kani Seed)'."
  - "1.2 (burst-234/2026-07-22): Add explicit ADR-020 Decision 3 cite to §Source Contract body (architectural anchor for BashTool risk floor invariant); confirm body has no stale 'ADR-018 Decision 6' reference. Update input-hash for ADR-020 v1.4 edit."
  - "1.1 (burst-233/2026-07-22): F-P133-06 — resolve stale BC-2.23.005 Category::CONFIGURATION contradiction flags: §Note on category field pointer removed (contradiction closed); §BC Contradictions Flagged section replaced with RESOLVED notice (BC-2.23.005 v1.1 = VAL, burst-232, consistent with this VP harness and error-taxonomy v1.31)."
  - "1.0 (D23/2026-07-22): Initial VP authored — D23 SS-23 BashTool risk-floor Kani proof (BC-2.23.005 vp_seed, ADR-020 Decision 3); Kani P1 Phase 6."
---

# VP-013: BashTool Risk Floor — ReadOnly and Low ActionRisk Always Return Err(E-TOOLS-007); Medium Is the Hard Floor

## Property Statement

For all `ActionRisk` variants `r` where `r` is `ReadOnly` or `Low`,
`check_risk_floor(r)` returns `Err(PregolyaError { code: "E-TOOLS-007", .. })`.
For all variants where `r` is `Medium` or `High`, `check_risk_floor(r)`
returns `Ok(())`. No code path permits `BashTool` to operate with `ActionRisk < Medium`.
The risk floor is an unconditional pure enum comparison — no runtime flag, no configuration
escape hatch.

**Formal invariant (DI-014):**
```
∀ r: ActionRisk:
  (r == ReadOnly ∨ r == Low) →
    check_risk_floor(r) == Err(PregolyaError { code: "E-TOOLS-007", category: VAL, .. })
    ∧ check_risk_floor(r) ≠ Ok(())

∀ r: ActionRisk:
  (r == Medium ∨ r == High) →
    check_risk_floor(r) == Ok(())
    ∧ check_risk_floor(r) ≠ Err(_)
```

The comparison `risk < ActionRisk::Medium` is the complete floor check. `ActionRisk` is a
bounded enum (finite variants); Kani exhaustively enumerates all variants. The `Ok(())`
arm is structurally unreachable when `r ∈ { ReadOnly, Low }`.

## Source Contract

- **BC:** BC-2.23.005 — BashTool — Sandboxed Shell Execution; Non-Lowerable Medium Risk Floor; BashOutput; 256 KiB Output Cap; 30 s Timeout; E-TOOLS-004/007 (VP-013 Kani Seed)
- **ADR anchor:** ADR-020 Decision 3 — Risk Tier Defaults and HITL Integration; defines the `BashTool` `Medium` risk floor as a framework safety invariant non-lowerable by application configuration (`BashTool` risk tier CANNOT be lowered below `Medium`; attempting to set `ReadOnly` or `Low` returns a configuration error at call time (inside `ToolConfig::override_risk`))
- **Postcondition 4 (Risk floor violation):** `ToolConfig::override_risk(ActionRisk::ReadOnly)` or `override_risk(ActionRisk::Low)` returns `Err(E-TOOLS-007 BashRiskTierViolation: BashTool risk tier cannot be lowered below Medium; attempted: '<tier>')`.
- **Invariant — Risk floor:** `ToolConfig::override_risk(ActionRisk::ReadOnly)` and `ToolConfig::override_risk(ActionRisk::Low)` on a `BashTool` instance ALWAYS return `Err(E-TOOLS-007)`. No code path allows these tiers on BashTool. Pure enum comparison (`risk < ActionRisk::Medium` → error) with no side effects.
- **Invariant — DI-014:** Configuration errors propagate as `Err`; risk floor violation is never silently accepted as if the floor were not present.
- **EC-004:** `override_risk(ReadOnly)` at call time → `Err(E-TOOLS-007)`; tool configuration fails at the `override_risk` call site before the registry or graph is involved.
- **EC-005:** `override_risk(Medium)` at call time → `Ok(ToolConfig)`; accepted because Medium is the hard floor; configuration succeeds at the call site.
- **TV-005:** `override_risk(ActionRisk::Low)` → `Err(E-TOOLS-007)` — `BashRiskTierViolation: BashTool risk tier cannot be lowered below Medium; attempted: 'Low'`.
- **TV-006:** `override_risk(ActionRisk::Medium)` → accepted; graph starts normally.

## BC Traceability

| BC | Title | Contribution |
|----|-------|-------------|
| BC-2.23.005 | BashTool — Sandboxed Shell Execution; Non-Lowerable Medium Risk Floor; BashOutput; 256 KiB Output Cap; 30 s Timeout; E-TOOLS-004/007 (VP-013 Kani Seed) | Primary VP obligation; risk floor invariant, E-TOOLS-007 error contract |
| BC-2.23.001 | ReadFileTool — PathGuard-Confined File Read; max_bytes 1 MiB Limit; E-TOOLS-001/002/008 | Contrast: ReadFileTool has `ActionRisk::ReadOnly` with no floor restriction; VP-013 is BashTool-specific |
| BC-2.05.007 | PreToolCallHook Dispatch — pre_invoke Contract; Approve/Deny/Edit/PendingHumanApproval; Fail-Closed Deny (VP-011 Kani Seed) | Related: ActionRisk::High (BashTool default) feeds ToolCallPreview.action_risk; VP-013 floor check runs at configuration time (inside `ToolConfig::override_risk`), before any registry registration or hook dispatch |

Specific anchors: BC-2.23.005 §Postcondition 4, §Invariant (Risk floor), §EC-004/005, §TV-005, §TV-006.

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| Model checking | Kani 0.67.0 | Yes — `ActionRisk` is a finite enum (bounded variants: ReadOnly, Low, Medium, High — 4 variants; `#[non_exhaustive]`); Kani exhaustively enumerates all variants via explicit bounded-integer encoding (0=ReadOnly, 1=Low, 2=Medium, 3=High); no Arbitrary derivation required | All ActionRisk variants; proves ReadOnly and Low → Err(E-TOOLS-007); Medium/High → Ok(()) |

**Kani async constraint (Kani 0.67.0 has NO native async support):**
`check_risk_floor(risk: ActionRisk) -> Result<(), PregolyaError>` is a pure-core
synchronous function in `pregolya-tools::tools::shell`. No `.await`, no Tokio runtime,
no I/O — it is a pure enum comparison followed by an `Err` or `Ok` constructor. The Kani
harness calls it directly.

**Enum exhaustiveness:** `ActionRisk` is marked `#[non_exhaustive]` per CLAUDE.md conventions.
The Kani harness covers all four canonical variants (`ReadOnly`, `Low`, `Medium`, `High`)
via explicit case enumeration. If `ActionRisk` gains new variants in future cycles, the
harness must be updated and the floor rule re-evaluated for each new variant. The
risk-floor invariant assumes that any variant with lower authorization than `Medium` should
trigger `E-TOOLS-007`; the harness verifies the current variant set exhaustively.

**Harness boundary:** The proof covers `check_risk_floor` as a standalone pure function
reached via the `ToolConfig::override_risk` call path. The floor validation is performed
inside `override_risk` (which takes `self` by value and returns `Result<ToolConfig, PregolyaError>`),
making it immediate and unavoidable at the call site; the effectful `ToolRegistry` is
excluded from Kani scope. The proof establishes that the floor check is correct for all
variants; the integration between `override_risk` usage and `ToolRegistry` is covered by
behavioral tests.

## Proof Harness Skeleton

Target file: `pregolya-tools/src/shell/bash.rs` (story S-1.22 implements BashTool in `shell/` subdirectory; `check_risk_floor` lives in `bash.rs`)

```rust
// pregolya-tools/src/proofs/risk_floor.rs
// Target fn: tools::shell::check_risk_floor

use crate::shell::bash::*;

/// VP-013: Risk Floor — ReadOnly and Low always return Err(E-TOOLS-007).
/// Proves: check_risk_floor(ReadOnly) == Err and check_risk_floor(Low) == Err.
///         check_risk_floor(Medium/High) == Ok.
#[kani::proof]
fn risk_floor_rejects_below_medium() {
    // Explicitly enumerate below-floor variants (ActionRisk is #[non_exhaustive])
    // — cannot use kani::any() if Arbitrary is not derived; explicit enumeration is safe

    // ReadOnly must be rejected
    let result_ro = check_risk_floor(ActionRisk::ReadOnly);
    kani::assert(
        matches!(result_ro, Err(PregolyaError { code: "E-TOOLS-007", .. })),
        "ReadOnly MUST return Err(E-TOOLS-007)"
    );
    kani::assert(result_ro.is_err(), "Ok is unreachable for ReadOnly");

    // Low must be rejected
    let result_low = check_risk_floor(ActionRisk::Low);
    kani::assert(
        matches!(result_low, Err(PregolyaError { code: "E-TOOLS-007", .. })),
        "Low MUST return Err(E-TOOLS-007)"
    );
    kani::assert(result_low.is_err(), "Ok is unreachable for Low");
}

/// VP-013 (positive): Medium and High pass the floor check.
#[kani::proof]
fn risk_floor_accepts_at_or_above_medium() {
    // Medium: the floor itself — must pass
    let result_med = check_risk_floor(ActionRisk::Medium);
    kani::assert(result_med.is_ok(), "Medium MUST pass the risk floor check");

    // High: the BashTool default — must pass
    let result_high = check_risk_floor(ActionRisk::High);
    kani::assert(result_high.is_ok(), "High MUST pass the risk floor check");
}

/// VP-013 (exhaustive): enumerate all known variants via symbolic choice.
/// This harness verifies the COMPLETE floor rule for all variants simultaneously.
/// Bounded-integer encoding (0=ReadOnly, 1=Low, 2=Medium, 3=High) is used
/// intentionally — no kani::Arbitrary derivation is required for ActionRisk.
#[kani::proof]
fn risk_floor_exhaustive_coverage() {
    // Encode ActionRisk as a bounded integer (0=ReadOnly, 1=Low, 2=Medium, 3=High)
    // 4 canonical variants per interface-definitions.md §PreToolCallHook
    let idx: u8 = kani::any();
    kani::assume(idx <= 3);

    let risk = match idx {
        0 => ActionRisk::ReadOnly,
        1 => ActionRisk::Low,
        2 => ActionRisk::Medium,
        _ => ActionRisk::High,
    };

    let result = check_risk_floor(risk);

    // Floor invariant: idx < 2 (ReadOnly or Low) → Err; idx >= 2 → Ok
    if idx < 2 {
        kani::assert(
            matches!(result, Err(PregolyaError { code: "E-TOOLS-007", .. })),
            "risk below Medium MUST return E-TOOLS-007"
        );
    } else {
        kani::assert(result.is_ok(), "risk at or above Medium MUST return Ok");
    }
}
```

## Feasibility Assessment

**Feasibility: HIGH**

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | Trivially bounded | `ActionRisk` is a 4-variant enum; explicit enumeration covers all variants in a single harness iteration |
| Proof complexity | Very Low | The function is a single comparison `risk < ActionRisk::Medium` followed by an `Err` or `Ok` constructor. No arithmetic, no loops, no I/O |
| Tool support | Supported | Kani 0.67.0; `check_risk_floor` is pure-core sync; enum comparison is trivially modeled by CBMC |
| Estimated proof time | < 1 min | 4-variant enum exhaustion; CBMC solves in seconds |

Key factors:
1. `check_risk_floor` is a pure-core synchronous function — no I/O, no async, no state.
   Kani 0.67.0 compatible without any adaptation.
2. The proof is an exhaustive enumeration over 4 variants. CBMC's SAT solver handles this as 4
   independent evaluations of a single boolean expression.
3. The risk-floor invariant is structurally imposed by the ordering of `ActionRisk` variants.
   If `ActionRisk` uses `#[repr(u8)]` with ordering `ReadOnly=0, Low=1, Medium=2, High=3`,
   the floor check `risk < ActionRisk::Medium` (i.e., `risk < 2`) is equivalent to a simple
   integer comparison — trivially CBMC-tractable.

**Known constraint:** `ActionRisk` is `#[non_exhaustive]`. Future variants added between
`Low` and `Medium` in the ordering would break the floor invariant unless explicitly handled
in `check_risk_floor`. The implementer must ensure the comparison uses the variant ordering,
not ordinal position, or uses an explicit allowlist of above-floor variants. The harness
must be updated whenever `ActionRisk` gains new variants.

**Note on `category` field:** error-taxonomy.md §Component: TOOLS (registered at D23) registers `E-TOOLS-007` with
`category: VAL` (configuration validation error — same reasoning as E-RETRY-004, E-SBXD-006,
E-SPLIT-001/002). The Kani harness assertion uses `code: "E-TOOLS-007"` for the primary
check; the `category` field in the `PregolyaError` struct MUST be `Category::Val` per the
error-taxonomy.md §Component: TOOLS. BC-2.23.005 §Postconditions (PC-4) category is `VAL` (burst-232) — consistent with this VP.

## Proof Obligations

- [ ] `check_risk_floor(risk: ActionRisk) -> Result<(), PregolyaError>` extracted as a named
  pure-core synchronous function in `pregolya-tools::tools::shell`; no side effects
- [ ] `ActionRisk::ReadOnly`, `ActionRisk::Low`, `ActionRisk::Medium`, `ActionRisk::High`
  are constructable as concrete values in Kani scope (4 canonical variants per interface-definitions.md §PreToolCallHook)
- [ ] `PregolyaError { code: "E-TOOLS-007", category: Category::Val, .. }` is the error
  returned for below-floor variants (consistent with error-taxonomy.md §Component: TOOLS `VAL` designation)
- [ ] Harness compiles under `cargo kani -p pregolya-tools` without CBMC failures
- [ ] All three proof harnesses pass within CI time budget (≤ 3 minutes for VP-013)
- [ ] TV-005 and TV-006 (BC-2.23.005) pass as unit tests before Phase 3 story delivery for SS-23

## BC Contradictions Flagged

**RESOLVED (burst-232, 2026-07-22):** BC-2.23.005 §Postconditions (PC-4) category corrected to `VAL`
(same burst that seeded VP-013). The prior `Category::CONFIGURATION` label was non-canonical
(not present in the 13-category axis per ADR-010). BC-2.23.005 §Postconditions (PC-4) category = VAL, consistent with
error-taxonomy.md §Component: TOOLS and this VP harness. No active contradictions.

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-07-22 | architect |
| Red Gate test authored | | test-writer |
| Red Gate test passes (implementation complete) | | implementer |
| Proof harness committed | | formal-verifier |
| Proof first passed | | formal-verifier |
| Locked (VERIFIED) | | formal-verifier |
