---
document_type: prd-supplement-error-taxonomy
level: L3
version: "1.25"
status: active
producer: product-owner
timestamp: 2026-07-18T00:00:00Z
changelog:
  - "1.25 (F-P111-01/FORM3-CENSUS, 2026-07-18): Gate #33 Form 3 wrapper-form census — full sweep of FerrochainError wrapper-form constructions (FerrochainError { code: E-COMP-NNN }) across all behavioral contracts. A wrapper-form site is a FAIL if the error code has placeholder tokens in its taxonomy Message Format and the site neither (a) provides an inline message: field, (b) has a full-form non-TV authoritative site elsewhere in the same BC (PASS-ABBREV rule), nor (c) is registered as a context-sourced exception. Census result: violations found across 17 BC files (fix burst split across pass-111 bursts 115a+115b). Violations fixed by approach: (1) Context-sourced exception registered: E-CORE-007 in BC-2.11.002, BC-2.11.003, BC-2.11.004 — <boundary> from ProvenanceTag.boundary_type and <content_type> from IngressContent variant discriminant, both deterministically available at GuardrailHook::evaluate() call site. (2) Inline message template added: E-RETRY-001 (BC-2.16.001 PC5), E-RETRY-002 (BC-2.16.002 PC5), E-CORE-001 (BC-2.01.001 PC6+EC-006), E-PROV-002 (BC-2.14.004 PC5/EC-003/TV-004), E-PROV-003 (BC-2.08.007 EC-004+TV-003; BC-2.08.001 EC-003), E-MEMORY-008 (BC-2.15.004 EC-004), E-GRAPH-006 (BC-2.03.001 EC-005), E-GRAPH-017 (BC-2.03.001 PC5+EC-006), E-CHKPT-001 (BC-2.04.001 EC-002), E-GRAPH-007 (BC-2.04.004 EC-003), E-CORE-005 (BC-2.04.006 EC-003; BC-2.08.004 PC5), E-CHKPT-005 (BC-2.04.006 EC-005), E-MCP-004 (BC-2.09.002 PC8), E-GRAPH-008 (BC-2.17.002 EC-002), E-PROV-001 (BC-2.08.004 PC3+EC-003), E-PROV-006 (BC-2.08.004 EC-002). Gate #33 bc-authoring-plan updated to v2.38: Form 3 grep added to Step A; wrapper-form discipline documented; E-CORE-007 registered in context-sourced exception registry. No taxonomy row changes — all defects were BC-body-side; taxonomy Message Format placeholders were already correctly specified."
  - "1.24 (F-P110-01/F-P110-02/CENSUS, 2026-07-18): CORRIGENDUM #4 for v1.23 false-claim correction AND two additional BC struct fixes found by full cross-anchor census. (1) F-P110-01 (MED, process-gap): The v1.23 changelog entry stated 'E-GRAPH-002 NoActiveInterrupt — taxonomy has two placeholders (`<run_id>` and `<run_status>`).' That claim is FALSE. The E-GRAPH-002 Message Format is 'NoActiveInterrupt: no interrupt is pending for run `<run_id>`' — ONE placeholder only: `<run_id>`. The BC-2.05.005 struct `{ thread_id, run_status }` carries `thread_id` ↔ `<run_id>` (registered alias) as the required placeholder field, and `run_status` as an EXTRA structured field (valid superset per PC3 audit utility — not a taxonomy placeholder). The v1.23 description was accurate about the actual defect and fix (9 sites missing `thread_id` were correctly identified and fixed in BC-2.05.005 v1.3) but the count-of-placeholders rationale was incorrect. No taxonomy Message Format change — E-GRAPH-002 row already correctly specifies one placeholder. Correction of process-gap in v1.23 rationale text only. (2) F-P110-02 (HIGH, process-gap): E-SBXD-001 WorkspaceEscape secondary anchor BC-2.13.004 TV-002 used `{ resolved: \"/etc/passwd\", root: \"/workspace\" }` (2-field form) missing `requested`. Root cause: TD-VSDD-060 sweep was anchored 'in-file' rather than 'across ALL anchor BCs' — the taxonomy E-SBXD-001 BC-Anchor cell lists BC-2.13.005 (primary) AND BC-2.13.004 (secondary). BC-2.13.005 Invariant-2 establishes canonical 3-field form `{ requested, resolved, root }` as authoritative for ALL sites across both anchors; BC-2.13.004 TV-002 diverged to 2-field. Fixed in BC-2.13.004 v1.2 (TV-002 expanded to `{ requested: \"/workspace/../etc/passwd\", resolved: \"/etc/passwd\", root: \"/workspace\" }`). (3) NEW FAIL (census-scope expansion): Full cross-anchor census with 3-grep Step A (primary Err(E-...) grep + secondary variant-name grep + third safety grep) found 34 struct-bearing codes total vs 30 in the prior scope. Four newly-scoped codes: E-GRAPH-009 (DuplicateNodeName, BC-2.02.001, PASS), E-GRAPH-014 (InterruptApprovalTimeout, BC-2.05.006, FAIL — EC-005 `{ tier, deadline_utc }` missing `run_id`; fixed in BC-2.05.006 v1.4 to `{ run_id: \"<run_id>\", tier, deadline_utc }`), E-CRON-002 (InvalidCronExpression, BC-2.12.004 EC-002, PASS — `{ field, value, reason }` covers all 3 taxonomy placeholders), E-SERVER-006 (ScheduleNotFound, BC-2.12.004 EC-005, PASS). No taxonomy row changes — all defects were on the BC body side."
  - "1.23 (F-P109-01/CENSUS, 2026-07-18): CORRIGENDUM #3 for v1.22 zero-remaining claim. The v1.22 entry stated 'Step A of the new census procedure found 7 further struct-bearing codes not previously scoped (E-GRAPH-002, E-GRAPH-008, E-GRAPH-010, E-GRAPH-013, E-SBXD-002, E-SERVER-015, E-CRON-001/003) — all assessed PASS under Steps B and C.' That 'all assessed PASS' claim is FALSE for E-GRAPH-002 and two additional codes. Root cause: v1.22 census assessed 'does some struct field exist?' rather than mapping each taxonomy placeholder to a named struct field AT EACH SITE. Three defects found by full Step A–C census under strengthened gate #33 v2.36 rules: (1) E-GRAPH-002 NoActiveInterrupt — taxonomy has two placeholders (`<run_id>` and `<run_status>`); PC1 correctly carried `{ thread_id, run_status }` (thread_id ↔ <run_id> registered alias); 9 of 10 sites (EC-001/002/003/004, TV-001/002/003/004/005) carried only `{ run_status }` or bare variant — missing thread_id. Fixed in BC-2.05.005 v1.3 (all 9 sites expanded to `{ thread_id, run_status: \"<value>\" }`). (2) E-MCP-002 McpTransportError — BC-2.09.001 TV-004 used `{ server: \"math\", ... }` as the SOLE struct site with no defining PC/EC full-struct form; `...` abbreviation failed the new PASS-ABBREV rule. Fixed in BC-2.09.001 v1.3 (TV-004 expanded to `{ server: \"math\", transport_error: \"connection refused\" }`). (3) E-SBXD-001 WorkspaceEscape — BC-2.13.005 TV-002 and TV-003 used `{ resolved: \"/etc/passwd\" }` (single field); taxonomy has `<resolved>` and `<root>` (2 placeholders); PC4 canonical form has 3 fields `{ requested, resolved, root }` — TV-002/TV-003 also failed intra-BC consistency (vs PC4/TV-001 3-field form) and placeholder coverage (missing `<root>`). Fixed in BC-2.13.005 v1.1. This is the third consecutive false completeness claim (v1.20 for 22-code scope; v1.21 for 17-PASS claim; v1.22 for 7 additional codes). No taxonomy row changes — all defects were on the BC body side; taxonomy Message Format placeholders were already correctly specified. Corrected full census table published in fix-burst-113 report."
  - "1.22 (F-P108-01/02/03/04, 2026-07-18): CORRIGENDUM for v1.21 sweep-completeness claim. The v1.21 entry stated '22 codes checked, 5 FAIL (fixed), 17 PASS.' That '17 PASS' claim is FALSE — 3 of those 17 codes were FAIL: E-PROV-010 ProviderChainExhausted (EC-004 used `{ providers_attempted, last_error }` where `last_error` combined two taxonomy placeholders `<last_error_code>/<last_provider>` into one field), E-CHKPT-004 EncryptionKeyRotationFailed (PC4 used `{ source: <reason> }` while PC5/EC-002/TV used `{ message: \"EncryptionKeyRotationFailed: ...\" }` — intra-BC field-name inconsistency), E-PROV-009 ToolCallDialectParseError (EC-002 used `{ dialect, reason }` where `reason` embedded the mid-message `<n>` offset placeholder, making it impossible to render the 4-placeholder taxonomy message from 2 fields). Root cause: the v1.21 sweep checked that struct fields EXISTED but did not verify CONSTRUCTIBILITY — specifically, (1) whether all struct sites in a BC use identical field names (intra-BC consistency), and (2) whether the struct field set is a SUPERSET of ALL distinct taxonomy placeholders with no multi-placeholder field combining two values. This gap has been codified as the STRUCT-PLACEHOLDER PARITY CENSUS procedure in bc-authoring-plan gate #33 (v2.35, F-P108-04). Corrected census for 22-code scope: 8 FAIL total across three bursts (v1.20: E-MEMORY-006; v1.21: E-GRAPH-011, E-GRAPH-007, E-GRAPH-001, E-GRAPH-004; v1.22: E-PROV-010, E-CHKPT-004, E-PROV-009 — all now fixed in respective BC files), 14 PASS. Additionally, Step A of the new census procedure found 7 further struct-bearing codes not previously scoped (E-GRAPH-002, E-GRAPH-008, E-GRAPH-010, E-GRAPH-013, E-SBXD-002, E-SERVER-015, E-CRON-001/003) — all assessed PASS under Steps B and C. No taxonomy row changes in this version — all 3 new defects were on the BC body side; taxonomy Message Format placeholders were already correctly specified."
  - "1.21 (F-P107-01, 2026-07-18): CORRIGENDUM for v1.20 sweep-completeness claim. The v1.20 entry stated '22 codes checked, 1 fixed, 21 PASS.' That claim is FALSE — E-GRAPH-011 was a same-class struct-field defect that wrongly passed the sweep. Root cause: EC-003 of BC-2.02.005 contained the phrase 'preserving the panic message as the error source' which caused the sweep to interpret the single 'source' field as containing both the source node name and panic text; the sweep passed E-GRAPH-011 without noticing the taxonomy message requires two distinct placeholders '<source_node>' and '<message>'. Additionally, the census rerun found three further same-class defects missed by the pass-106 sweep: E-GRAPH-007 (struct { key } missing '<node_id>'); E-GRAPH-001 (struct { channel } or { channel, reason } missing '<task_ids>' and '<n>'); E-GRAPH-004 (struct { channel, writer } missing '<n>'). Corrected census result: 22 codes checked, 5 FAIL (E-MEMORY-006 fixed in v1.20; E-GRAPH-011, E-GRAPH-007, E-GRAPH-001, E-GRAPH-004 fixed in BC versions bumped this burst), 17 PASS. Error-taxonomy.md row for E-GRAPH-011 stays as-is — the taxonomy message already correctly specifies two placeholders '<source_node>' and '<message>'; BC-2.02.005 was the defect side. No taxonomy row changes this version."
  - "1.20 (OBS-P106-A, 2026-07-18): E-MEMORY-006 Message Format corrected. Was: `InsufficientPrivilege: operation '<operation>' requires AdminContext; caller has <caller_privilege>`. Now: `InsufficientPrivilege: operation '<operation>' requires <required>`. Defect: 'AdminContext' was hardcoded where BC-2.15.003 EC-005 parameterizes the `required` field; `<caller_privilege>` had no corresponding struct field (`{ operation, required }` only). Gate #33 BC-wins-on-divergence rule applied; taxonomy corrected to map 1:1 to struct fields. Sibling sweep (all struct-bearing MEMORY codes): E-MEMORY-002 (`{ backend, path }` ↔ `<backend>/<path>` PASS), E-MEMORY-003 (`{ requested_scope, caller_identity }` ↔ `<caller_identity>/<requested_scope>` PASS), E-MEMORY-005 (`{ user_id, ..., backend_error }` ↔ `<user_id>/<reason>` PASS), E-MEMORY-007 (`{ reason }` + ns/key from write-op context ↔ `<ns>/<key>/<reason>` PASS per F-P78-01 authority). Full sweep of all struct-bearing taxonomy codes: 22 codes checked, 1 fixed (this entry), 21 PASS. See fix-burst-110 report for per-code sweep table."
  - "1.19 (F-P105-01/OBS-P105-A, 2026-07-18): (1) F-P105-01 — SECURITY category description corrected: was 'Workspace escape, sandbox policy enforcement'; 'sandbox policy enforcement' contradicts E-SBXD-002 PolicyNotEnforceable (POLICY, not SECURITY — policy-enforcement rejections are POLICY by definition) and omits 2 of 3 SECURITY members (E-GRAPH-013 InsufficientApproverRole, E-MEMORY-007 MemoryWriteGuardDenied). New description: 'Workspace/sandbox escape; approver-role authorization failure; agent-memory write injection prevention' — spans all three SECURITY members: E-SBXD-001 WorkspaceEscape (workspace/sandbox path escape per BC-2.13.005), E-GRAPH-013 InsufficientApproverRole (HITL risk-tier approver-role authorization per BC-2.05.006), E-MEMORY-007 MemoryWriteGuardDenied (agent-controlled write injection prevention — prompt-injection prefixes, invisible-Unicode — per BC-2.15.005). Sibling sweep (all 11 other category rows): VAL/AUTH/RATE/TIMEOUT/TRANSPORT/INTERNAL/DURABILITY/POLICY/TOOL/CONCURRENCY/TENANCY descriptions verified — no contradictions found; descriptions accurate for all non-SECURITY rows. (2) OBS-P105-A — SECURITY vs POLICY authorization-failure categorization rule documented as a new blockquote note after the Error Categories table. Adjudication: E-GRAPH-013 SECURITY (HITL approval gate — bypass could authorize harmful high-risk action; BC-2.05.006), E-MEMORY-003 POLICY (cross-scope data isolation policy; BC-2.15.002), E-MEMORY-006 POLICY (admin-only erasure capability constraint; BC-2.15.003). Rule: SECURITY guards boundaries whose bypass enables attack vectors; POLICY enforces privilege/data-access constraints for otherwise-legitimate callers. (3) Sibling-sweep follow-up (same burst, production-grade default — narrow-but-no-contradiction is a blocker, not an advisory): four category descriptions broadened to span full member sets — TIMEOUT 'Outbound connection or response timeout' → 'Outbound connection/response timeout; internal lock acquisition timeout' (adds E-SERVER-016 IdempotencyLockTimeout); TRANSPORT 'TCP reset, TLS error, DNS failure' → 'TCP/TLS/DNS connection failure; socket bind failure; unstructured or unparseable HTTP error body' (adds E-PROV-008 ProviderHttpError, E-MCP-005 McpServerBindFailed); DURABILITY 'Checkpoint write or read failure' → 'Persistent storage I/O failure or capacity exhaustion (checkpoints, memory-store, run-store, journal)' (adds E-MEMORY-002 StorageFull, E-MEMORY-005 ErasurePartialFailure, E-MEMORY-008 MemoryStoreReadFailed, E-SERVER-014 RunStoreFailed, E-BUDGET-002 JournalWriteFailed); CONCURRENCY 'Conflicting concurrent writes to same channel' → 'Conflicting concurrent state — channel writes or resource-state conflicts (run, thread, execution)' (adds E-SERVER-007 ThreadAlreadyExists, E-SERVER-012 ConcurrentRun, E-SERVER-015 RunAlreadyExecuting). No version bump — same logical fix burst as items (1) and (2)."
  - "1.18 (F-P91-04, 2026-07-17): Mint E-MEMORY-008 (MemoryStoreReadFailed, DURABILITY, broken, BC-2.15.004 EC-004/TV-008, RetryHint Maybe). Adjudication: BC-2.15.004 EC-004 cited E-MEMORY-002 StorageFull for a backend read I/O failure — StorageFull is a write-capacity semantic (no remaining capacity), categorically wrong for a read I/O error. No existing MEMORY code covered read I/O failure. E-MEMORY-008 minted as the correct code. DURABILITY category (storage read failure = durability event); RetryHint Maybe = DURABILITY default (transient SQLite I/O can succeed on retry; no divergence, no blockquote entry needed). Near-name check: MemoryStoreReadFailed vs MemoryWriteGuardDenied (E-MEMORY-007) — distinct read/write domains, PASS. MEMORY namespace: 7→8 live codes. Disposition census: 85→86 (blanket 26→27: E-MEMORY-* 7→8; 43 HTTP + 16 individual + 27 blanket = 86). BC-2.15.004 EC-004 and TV-008 updated to cite E-MEMORY-008."
  - "1.17 (F-P88-01, 2026-07-17): Version/changelog/timestamp propagation for pass-87 burst body changes. Pass-87 (bc-authoring-plan v2.21) renamed §'Error Category Codes' → §'Error Categories' and normalized input-hash from legacy 64-char SHA-256 to 7-char MD5 ('f766c52'). Those body modifications landed without a corresponding version/timestamp bump, leaving the file at v1.16/2026-07-15 despite the section-rename change. Correction applied: version 1.16 → 1.17, timestamp → 2026-07-17. No semantic content changes in this entry."
  - "1.16 (F-P78-SWEEP/D18-P78-A/D18-P77-B, 2026-07-15): Full gate #33 semantic sweep — 13 Message Format corrections. (1) E-CORE-006: was 'nested invoke/stream call depth <depth> exceeded recursion_limit <limit>'; corrected to 'recursion limit exceeded at depth <depth>' (BC-2.01.003 wins on content; BC also corrected D18-P78-A prefix at 3 sites). (2) E-CHKPT-002: was 'checkpoint ID <new_id> is not strictly greater than current <current_id>' (regression-comparison semantics); corrected to 'checkpoint_id must be monotonic: random UUID rejected' (BC-2.04.003 EC-003 wins; completely different semantic — UUID rejection not regression). (3) E-CHKPT-004: was 'checkpoint state encryption key rotation error: <reason>'; corrected to '<reason>' (BC-2.04.007 wins; no wrapper). (4) E-CHKPT-007: was elaborate key/path/migration message; corrected to 'missing cipher header: blob may be unencrypted' (BC-2.04.007 EC-004 wins). (5) E-SERVER-007: placeholder corrected from '<thread_id>' to '<id>' (BC-2.12.001 wins). (6) E-PROV-002: was 'streaming connection to <provider> timed out after <ms>ms'; corrected to 'stream chunk timeout after <duration>' (BC-2.08.007 PC1 wins on content; no <provider> in BC message; <duration> more precise than <ms>ms; BC also corrected D18-P78-A). (7) E-PROV-004: was \"'<provider>' rejected API key — check credentials\"; corrected to 'authentication failed' (BC-2.08.004 EC-001 wins). (8) E-PROV-005: was 'provider response did not match expected JSON schema: <path> — <reason>'; corrected to '<reason>' (BC-2.08.003 EC-002 wins). (9) E-PROV-007: was \"model refused to generate structured output — refusal_message: '<message>'\"; corrected to '<refusal_message>' (BC-2.08.003 EC-001 wins; placeholder renamed). (10) E-PROV-008: was single form 'provider <provider> returned HTTP <status>: <body_preview>'; now two forms per BC-2.08.004 EC-004/EC-005. (11) E-PROV-009: was '<dialect> failed to deserialize tool calls from content: <reason>'; corrected to '<dialect> <element> payload is not valid JSON at response offset <n>: <parse_error>' (BC-2.08.013 PC8 wins on structure; Hermes-specific form generalized). (12) E-PROV-010: was 'all <chain_length> providers in fallback chain failed — last error: <last_error>'; corrected to 'all <N> providers in fallback chain failed; last error: <last_error_code>/<last_provider>' (BC-2.08.014 PC5 wins: semicolon separator, <N> placeholder, split last-error field). (13) E-BUDGET-001: was 'run <run_id> halted; token budget of <limit> exceeded at <actual> tokens'; corrected to 'run halted: budget ceiling reached' (BC-2.10.003 PC5 wins)."
  - "1.15 (F-P78-01/F-P78-04/D18-P78-A, 2026-07-15): F-P78-01 — E-MEMORY-007 Message Format corrected: was 'MemoryWriteGuardDenied: write guard denied '<operation>' for namespace '<namespace>' key '<key>': <reason>' (wrong word order, spurious <operation> placeholder, colon separator); corrected to 'MemoryWriteGuardDenied: write to namespace '<ns>' key '<key>' denied — <reason>' to match BC-2.15.005 PC2 authoritative message (em-dash separator, ns shorthand, no operation field). F-P78-04 (D18-P78-A adjudication) — E-CHKPT-008 Message Format annotation for PC6 case now consistent: BC-2.04.008 PC6 updated to add FtsLimitZero: prefix (universal <Name>: <detail> convention; BC corrected, taxonomy unchanged). Raise-condition annotations for both codes verified correct vs BC EC/TV predicates."
  - "1.14 (F-P77-01, 2026-07-15): E-SBXD-006 semantic correction — rewritten from regex model to wildcard/exact-name model per BC-2.13.007 PC5/EC-003/EC-005/TV-005/TV-006. Message Format updated from 'is not a valid regex pattern — <reason>' to 'contains wildcard characters — only exact variable names are supported in v1'. Raise-condition annotation updated: triggered by `*` or `?` in any allowlist entry at construction time (not by regex compilation failure). All regex language removed. VAL category and RetryHint Never unchanged. D18-P77-B: gate #33 SEMANTIC-AGREEMENT sub-check extended in bc-authoring-plan.md v2.17."
  - "1.13 (pass-72 fix, 2026-07-15): F-P72-07 — E-MEMORY-007 rationale corrected from sandbox-escape boilerplate to prompt-injection domain. Previous rationale stated 'workspace security policy (sandbox escape prevention, untrusted-tool output isolation)' — that text is boilerplate from E-SBXD-001 and is wrong for memory write guarding. Correct domain per BC-2.15.005 EC-001–EC-002 + ADR-012 Decision 2: prompt-injection prefixes (Human:/Assistant:/Ignore-previous-instructions) and invisible-Unicode characters (U+200B–U+200F, U+FEFF, U+202A–U+202E) in agent-controlled memory and skill writes."
  - "1.12 (D20 TOUCH-UP burst — CORRIGENDUM): Residue 2 census split correction. The disposition census split stated in v1.11 (44 HTTP + 15 individual + 26 blanket = 85) is wrong; the correct split is 43 HTTP + 16 individual + 26 blanket = 85. Root cause: interface-definitions.md v2.19 (F-P69-01, ADV-P1D-PASS-69) moved E-CORE-004 from the HTTP status table to an individual omission note (HTTP 44→43, individual 11→12), but error-taxonomy.md v1.10 reported 'disposition census unchanged at 78' without tracking this code migration. When D20 sub-burst 2 ran, this file computed from the stale pre-migration baseline (44 HTTP + 11 individual) rather than the post-migration baseline (43 HTTP + 12 individual), producing a split that was off-by-one in both columns. Verified correct partition: 43 codes in HTTP status table rows (counted in interface-definitions.md §HTTP Status Codes) + 16 codes in named individual omission notes + 26 blanket-covered codes (E-MCP-* 5 + E-SBXD-* 6 + E-RETRY-* 4 + E-BUDGET-* 2 + E-MEMORY-* 7 + E-SPLIT-* 2 = 26) = 85 total live codes. No code classification changes — this is a census accounting fix only."
  - "1.11 (D20 INTEGRATE sub-burst 2): Seven new error codes registered for D20 integration BCs. (1) E-CHKPT-008 (FtsLimitZero, VAL) and E-CHKPT-009 (Fts5Unavailable, INTERNAL) — split from original ambiguous single code in BC-2.04.008 EC-006 (one VAL + one INTERNAL; adjudicated: different categories require different codes per taxonomy governance). CHKPT namespace now 9 live codes. (2) E-PROV-009 (ToolCallDialectParseError, VAL, BC-2.08.013) — dialect deserialization failure. (3) E-PROV-010 (ProviderChainExhausted, POLICY, BC-2.08.014) — all fallback chain providers exhausted. PROV namespace now 10 live codes. (4) E-MCP-005 (McpServerBindFailed, TRANSPORT, BC-2.09.006, RetryHint: Never) — TRANSPORT divergence: socket bind failure is non-recoverable by retry; added as 6th entry in RetryHint precedence blockquote. MCP namespace now 5 live codes. (5) E-SBXD-006 (InvalidEnvAllowlistPattern, VAL, BC-2.13.007) — regex compile failure at SandboxConfig construction. SBXD namespace now 6 live codes. (6) E-MEMORY-007 (MemoryWriteGuardDenied, SECURITY, BC-2.15.005) — write guard denied. MEMORY namespace now 7 live codes. Disposition census 78→85: 44 HTTP table rows, 15 individual omission notes (+4: E-CHKPT-008, E-CHKPT-009, E-PROV-009, E-PROV-010), 26 blanket library-layer coverage entries (+3: E-MCP-005, E-SBXD-006, E-MEMORY-007)."
  - "1.10 (ADV-P1D-PASS-70): F-P70-02 — correct stale 401 note in E-SERVER-004 blockquote: 'now marked reserved (no E-code maps there in v1)' → 'carries no directly-emitted E-code in v1 (E-PROV-004 AUTH→401 is a categorical fallback surfaced embedded in Run.error, never a direct terminal response)'. Cross-doc sweep (5 other taxonomy-to-interface-definitions notes): E-PROV-007 POLICY→403 fallback note vs 403 row — PASS (interface-definitions omission note for E-PROV-007 consistent with taxonomy note); E-PROV-008 TRANSPORT→502 fallback note vs 502 row — PASS (502 row correctly lists E-PROV-008 as categorical fallback only; aligned). No other stale cross-doc notes found. Disposition census unchanged at 78."
  - "1.9 (ADV-P1D-PASS-66): F-P66-03 — RETIRE E-SERVER-005 (CorsRejected, POLICY): tombstone row added; BC-2.12.005 PC2/TV-001 specifies CORS denial as silent header-omission — no error body is ever emitted; code was never raised. SERVER namespace: 14 live codes (was 15). Disposition census 79→78: 44 HTTP table rows, 11 individual omission notes, 23 blanket library-layer coverage. F-P66-01 — re-anchor E-MCP-003 (McpNotImplemented, VAL) from BC-2.09.005 (connection-lifecycle scope; method-invocation surface absent) to BC-2.09.001 (list_tools discovery path — server returns JSON-RPC -32601 MethodNotFound when tools/list is not implemented); EC-006/TV-008 added to BC-2.09.001 in the same burst."
  - "1.8 (ADV-P1D-PASS-56-COMPLETION): Gate #30 second-pass drain — three new codes minted to resolve all remaining TBD-* placeholders and codeless constructions. (1) E-PROV-008 (ProviderHttpError, TRANSPORT, broken, BC-2.08.004) — resolves TBD-E-PROV-HTTP placeholder in BC-2.08.004 EC-004/EC-005/TV-004/TV-005; covers generic provider HTTP 5xx and unparseable error-body responses; both sites share TRANSPORT category so one code is correct (task-1 discipline: split only on genuine category divergence). Disposition census: +1 HTTP table row (502 row). (2) E-CORE-007 (GuardrailHookPanic, INTERNAL, broken, BC-2.11.002 / BC-2.11.003 / BC-2.11.004) — panic in GuardrailHook::evaluate is an invariant violation (programming error) caught at the ingress boundary; fail-closed semantics. CORE namespace now 7 live codes. Disposition census: +1 individual omission note. (3) E-CHKPT-007 (CipherHeaderMissing, INTERNAL, broken, BC-2.04.007) — read of an unencrypted legacy blob in an encrypted store is an INTERNAL invariant violation (expected cipher header absent). CHKPT namespace now 7 live codes. Disposition census: +1 HTTP table row (500 row). Total disposition census 76→79: 45 HTTP table rows, 11 individual omission notes, 23 blanket library-layer coverage."
  - "1.7 (ADV-P1D-PASS-56): F-P56-01 — mint E-CORE-006 (RecursionLimitExceeded, INTERNAL, broken, BC-2.01.003). The Runnable-layer nested-call-depth guard returned a codeless FerrochainError while its graph-engine counterpart E-GRAPH-017 (minted pass 49) carried a code. E-CORE-006 fills this gap. INTERNAL/Never — matches INTERNAL category default (no divergence, no per-code RetryHint column needed). BC-2.01.003 PC5, invariant §layer-disambiguation, EC-004, TV-004 all updated to carry code: E-CORE-006. CORE namespace now has 6 live codes. Disposition census 75→76 (10 individual omission notes: +E-CORE-006 library-layer note)."
  - "1.6 (ADV-P1D-PASS-49): F-P49-02 — mint E-GRAPH-017 (GraphRecursionLimitExceeded, POLICY, broken, BC-2.03.001, Never). Graph super-step ceiling was unported: BC-2.03.001 and BC-2.08.002 both referenced a 'configurable step limit' with no defined error code. E-GRAPH-017 is the halt signal when config.recursion_limit (default 25) super-steps are exhausted in one invocation segment. POLICY/Never — same pattern as E-RETRY-001/002 (limit-exhaustion halt; retrying without changing the config/graph will produce the same result). No RetryHint divergence (POLICY default Never is correct). No GRAPH-namespace POLICY annotation change needed (POLICY already present: E-GRAPH-002, E-GRAPH-014, E-GRAPH-016)."
  - "1.5 (ADV-P1D-PASS-34): F-P34-02 — mint E-RETRY-004 (InvalidRetryLimit, VAL, broken, BC-2.16.001, Never). BC-2.16.001 EC-003/TV-004 had incorrectly reused E-RETRY-003 (CircuitBreakerOpen, POLICY, Later) for a construction-time misconfiguration. E-RETRY-004 is the correct code: VAL category, RetryHint Never (matches VAL default — not a divergence). No change to E-RETRY-003 (CircuitBreakerOpen remains sole owner in BC-2.16.003)."
  - "1.4 (ADV-P1D-PASS-29): F-P29-02 — add E-CRON-003 (ScheduleQueueFull) to the divergences blockquote as the 5th documented divergence. Decision: option (a) — add to blockquote with BC-2.12.004-anchored rationale (queue-full is transient capacity condition, backoff-recoverable → Later overrides POLICY Never). No category churn."
  - "1.3 (ADV-P1D-PASS-28): F-P28-01 relabel category-table RetryHint column to 'Default RetryHint'; add per-code-authoritative precedence rule. OBS-P28-3 mint E-PROV-007 (StructuredOutputRefused, POLICY, Never) anchored to BC-2.08.003 — OpenAI structured-output refusal path now carries a machine-readable code."
  - "1.2 (ADV-P1D-PASS-27): F-P27-02 recategorize E-CHKPT-004 SECURITY→INTERNAL (BC-2.04.007 authoritative; key rotation is an internal invariant failure, not a security policy rejection)."
  - "1.1 (ADV-P1D-PASS-25): F-P25-02 recategorize E-SERVER-004 AUTH→POLICY; correction note added inline."
phase: 1a
inputs:
  - .factory/specs/prd.md
  - .factory/specs/domain-spec/invariants.md
  - .factory/comparative/COMPARATIVE-ASSESSMENT.md
input-hash: "87b1e6b"
traces_to: prd.md
primary_consumers: [implementer, test-writer]
---

# Error Taxonomy: ferrochain

> PRD supplement — extracted from PRD Section 5.
> Source: CONFLICT-6 (adopt adk-rust P-01/P-04 2D component×category pattern).
> Codes follow convention: `E-<COMPONENT>-<NNN>` where COMPONENT is the crate abbreviation.
> All errors are `FerrochainError { component: Component, category: Category, retry_hint, code, message }`.

## Error Categories

| Category Code | Category | Description | Default RetryHint |
|--------------|----------|-------------|-------------------|
| VAL | Validation | Input shape, type, range constraint violations | Never |
| AUTH | Authentication | Credentials absent, expired, or invalid | Maybe (refresh token) |
| RATE | Rate Limit | Provider or server rate limit exceeded | Later (backoff required) |
| TIMEOUT | Timeout | Outbound connection/response timeout; internal lock acquisition timeout | Later |
| TRANSPORT | Transport | TCP/TLS/DNS connection failure; socket bind failure; unstructured or unparseable HTTP error body | Later |
| INTERNAL | Internal | Assertion-like invariant violation; programming error | Never |
| DURABILITY | Durability | Persistent storage I/O failure or capacity exhaustion (checkpoints, memory-store, run-store, journal) | Maybe |
| POLICY | Policy | Policy constraint rejected the operation | Never (policy change required) |
| TOOL | Tool | Tool execution error (includes MCP ToolException) | Maybe |
| CONCURRENCY | Concurrency | Conflicting concurrent state — channel writes or resource-state conflicts (run, thread, execution) | Never |
| SECURITY | Security | Workspace/sandbox escape; approver-role authorization failure; agent-memory write injection prevention | Never |
| TENANCY | Tenancy | Session address collision, cross-tenant access attempt | Never |

> **RetryHint precedence rule (F-P28-01, ADV-P1D-PASS-28):** The "Default RetryHint" column above applies only to codes whose per-code catalog row omits a RetryHint. Where a per-code catalog row specifies a RetryHint, the per-code value is authoritative — it overrides the category default. Intentional divergences must carry a BC-anchored rationale in the per-code row or a correction note. Examples: E-RETRY-003 (CircuitBreakerOpen, POLICY) carries `Later(<reset_timeout>)` because the circuit-breaker cool-down semantics of BC-2.16.003 override the POLICY `Never` default — the breaker trip is a temporary state with a known reset horizon; E-CRON-003 (ScheduleQueueFull, POLICY) carries `Later` because schedule queue overflow is a transient capacity condition — the next firing cycle will likely have capacity, making `Later` the correct semantics and a justified override of the POLICY `Never` default per BC-2.12.004 (F-P29-02, ADV-P1D-PASS-29); E-MEMORY-002/005 and E-BUDGET-002 (DURABILITY codes) carry `Never` because their specific failure modes (storage-full, partial-erasure, journal-write) are non-recoverable by retry without operator intervention. Divergences are intentional; the per-code value is the implementation contract. E-MCP-005 (McpServerBindFailed, TRANSPORT) carries `Never` because a socket bind failure (EADDRINUSE, EACCES) requires operator configuration change — the same bind address/port will fail immediately on retry; `Later` (TRANSPORT default) would be misleading per BC-2.09.006 (D20 sub-burst 2).

> **Authorization-failure categorization rule (OBS-P105-A, 2026-07-18):** Three authorization-failure codes span two categories: E-GRAPH-013 InsufficientApproverRole is **SECURITY**; E-MEMORY-003 ScopeAccessDenied and E-MEMORY-006 InsufficientPrivilege are **POLICY**. The split is intentional — the deciding criterion is whether insufficient authorization could enable a *security attack vector* vs. whether it enforces a *capability or data-access policy constraint*. **SECURITY** applies when the authorization check guards a boundary whose bypass could allow a concrete security violation: E-GRAPH-013 guards HITL risk-tier approval — a caller lacking the required approver role could authorize a high-risk external action if not blocked (BC-2.05.006 risk-tier enforcement gate). **POLICY** applies when the check enforces a data-access or operational capability rule for an otherwise-legitimate caller: E-MEMORY-003 enforces the cross-scope memory isolation policy — caller identity cannot write to another owner's scope (BC-2.15.002); E-MEMORY-006 enforces the admin-only GDPR erasure capability constraint (BC-2.15.003). Decision heuristic: if bypassing the check would allow an *attack vector* (harmful action approval, data poisoning, workspace escape) → **SECURITY**; if it rejects a *privileged operation* for a caller who lacks the required capability or scope right → **POLICY**.

## Severity Definitions

| Severity | Meaning | Surface Behavior |
|----------|---------|-----------------|
| broken | Cannot continue current operation; caller must handle | Non-zero exit / Err propagation |
| degraded | Partial result possible; caller may choose to surface partial data | Err with partial payload |
| cosmetic | Display or formatting issue only | Warning log; operation succeeds |

## Error Catalog

### Component: CORE (ferrochain-core)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-CORE-001 | VAL | broken | BC-2.01.001 | `StrictContentBlockValidation: block at position <n> has unrecognized type tag '<type>'; not in KNOWN_BLOCK_TYPES — use lenient deserialization for NonStandard passthrough` |
| E-CORE-002 | VAL | broken | BC-2.01.002 | `Message role '<role>' is not a recognized message type` |
| E-CORE-003 | VAL | broken | BC-2.01.003 | `Runnable input type mismatch: expected '<expected>', got '<actual>'` |
| E-CORE-004 | INTERNAL | broken | BC-2.01.004 | `Pipe composition failed: type boundary mismatch between stage <n> output and stage <n+1> input` |
| E-CORE-005 | VAL | broken | BC-2.14.006 | `Validation failed for '<field>': <reason>` |
| E-CORE-006 | INTERNAL | broken | BC-2.01.003 | `RecursionLimitExceeded: recursion limit exceeded at depth <depth>` |
| E-CORE-007 | INTERNAL | broken | BC-2.11.002 (secondary: BC-2.11.003, BC-2.11.004) | `GuardrailHookPanic: GuardrailHook::evaluate panicked at <boundary> for content type '<content_type>' — fail-closed: content blocked, not forwarded to model context` — **(ADV-P1D-PASS-56-COMPLETION):** A panic in a registered `GuardrailHook::evaluate` call is caught at the ingress boundary and treated as an invariant violation (programming error in the hook implementation). Fail-closed semantics: the content that triggered the panic is treated as rejected. INTERNAL/Never — matches INTERNAL category default. BC-2.11.002 is the primary anchor (tool-result ingress); BC-2.11.003 (RAG chunk ingress) and BC-2.11.004 (memory item ingress) share the same code since the panic-and-fail-closed pattern is identical across all three guardrail ingress boundaries. |

### Component: GRAPH (ferrochain-graph)

> **GRAPH reconciliation note (ADV-P1D-FIX-1 burst, 2026-07-14):** BC authors (ss-02/ss-05)
> independently assigned E-GRAPH-NNN codes without consulting this taxonomy, producing 6+
> collisions where the same code had different meanings in BCs vs taxonomy. Resolution:
> E-GRAPH-001..006 taxonomy meanings are kept stable (source of truth). All colliding BC
> usages are renumbered to E-GRAPH-007..014 below. E-GRAPH-003 renamed from "Node not
> found" to "UnknownRoutingTarget" (functionally identical; anchor corrected to BC-2.02.005
> which is the primary routing BC). E-GRAPH-004 renamed from "NamedBarrierValue unexpected
> keys" to "DuplicateBarrierWrite" (BC-2.02.003 EC-003 updated to use this code).
> Process-gap follow-up: Phase 2 backlog story for a global cross-component collision lint.

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-GRAPH-001 | CONCURRENCY | broken | BC-2.03.002 | `InvalidUpdateError: concurrent writes to LastValue channel '<channel>' from tasks [<task_ids>] in super-step <n>` |
| E-GRAPH-002 | POLICY | broken | BC-2.05.005 | `NoActiveInterrupt: no interrupt is pending for run '<run_id>'` |
| E-GRAPH-003 | VAL | broken | BC-2.02.005 | `UnknownRoutingTarget: node '<node_id>' is not registered in the compiled graph` |
| E-GRAPH-004 | VAL | broken | BC-2.02.003 | `DuplicateBarrierWrite: NamedBarrierValue channel '<channel>' received more than one write from writer '<writer>' in super-step <n>` |
| ~~E-GRAPH-005~~ | ~~POLICY~~ | ~~broken~~ | ~~BC-2.10.003~~ | ~~`BudgetCeiling: ...`~~ — **RETIRED** (ADV-P1D-PASS-2 fix, 2026-07-14): budget errors belong in the BUDGET component namespace. E-GRAPH-005 is superseded by E-BUDGET-001. Code never shipped; tombstone per append-only numbering policy. |
| E-GRAPH-006 | INTERNAL | broken | BC-2.03.001 | `BspDeterminismViolation: reducer order constraint violated — contact maintainers with run_id '<run_id>'` |
| E-GRAPH-007 | VAL | broken | BC-2.02.001 | `UnknownChannelKey: node '<node_id>' returned write for key '<key>' which is not registered in the state schema` |
| E-GRAPH-008 | VAL | broken | BC-2.02.001 | `UnreachableGraph: <reason> (e.g., "no entry edge from START")` |
| E-GRAPH-009 | VAL | broken | BC-2.02.001 | `DuplicateNodeName: node '<name>' is already registered in this graph` |
| E-GRAPH-010 | VAL | broken | BC-2.02.003 | `UnknownBarrierWriter: NamedBarrierValue channel '<channel>' declares writer '<writer>' which is not a registered node` |
| E-GRAPH-011 | INTERNAL | broken | BC-2.02.005 | `ConditionalEdgePanic: routing function for edge from '<source_node>' panicked: <message>` |
| E-GRAPH-012 | VAL | broken | BC-2.02.005 | `UnmappedRouteKey: path_fn returned symbolic key '<key>' not found in path_map` |
| E-GRAPH-013 | SECURITY | broken | BC-2.05.006 | `InsufficientApproverRole: action requires role '<required>'; caller presented role '<provided>'` |
| E-GRAPH-014 | POLICY | broken | BC-2.05.006 | `InterruptApprovalTimeout: interrupt for run '<run_id>' (tier '<tier>') expired at deadline '<deadline_utc>' without receiving required approval` |
| E-GRAPH-015 | VAL | broken | BC-2.05.004 | `NoParentGraph: Command.PARENT is only valid inside a subgraph execution context; no parent graph is active` |
| E-GRAPH-016 | POLICY | broken | BC-2.05.001 (secondary: BC-2.10.004) | `InterruptWithoutCheckpointer: interrupt() requires a CheckpointSaver attached to the graph; no durable state is available to park the interrupted run` |
| E-GRAPH-017 | POLICY | broken | BC-2.03.001 | `GraphRecursionLimitExceeded: run '<run_id>' halted after super-step <step>; step count exceeded recursion_limit <limit> — check graph for unintended cycles` |

### Component: CHKPT (ferrochain-checkpoint)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-CHKPT-001 | DURABILITY | broken | BC-2.04.001 | `CheckpointWriteFailed: put_writes for task '<task_id>' failed — backend error: <backend_error>` |
| E-CHKPT-002 | INTERNAL | broken | BC-2.04.003 | `MonotonicClockRegression: checkpoint_id must be monotonic: random UUID rejected` |
| E-CHKPT-003 | DURABILITY | broken | BC-2.04.005 | `CheckpointReadFailed: cannot restore state for thread '<thread_id>' checkpoint '<checkpoint_id>': <reason>` |
| E-CHKPT-004 | INTERNAL | broken | BC-2.04.007 | `EncryptionKeyRotationFailed: <reason>` — **Category correction (F-P27-02, ADV-P1D-PASS-27):** was SECURITY → INTERNAL. BC-2.04.007 constructs this error with `category: INTERNAL` throughout (PC4, PC5, EC-001, EC-002); key rotation failure is an internal invariant violation (programming/config error), not a security policy rejection. BC is authoritative. |
| E-CHKPT-005 | TENANCY | broken | BC-2.04.006 | `SessionAddressCollision: operation with (thread_id='<t>', ns='<ns>') conflicts with existing session — triple must be unique` |
| E-CHKPT-006 | INTERNAL | broken | BC-2.05.001 | `SerializationFailed: interrupt_value cannot be serialized by the configured checkpoint serializer — type contract violation (programming error)` |
| E-CHKPT-007 | INTERNAL | broken | BC-2.04.007 | `CipherHeaderMissing: missing cipher header: blob may be unencrypted` — **(ADV-P1D-PASS-56-COMPLETION):** Raised when `EncryptedSerializer` reads a stored blob that lacks the cipher header sentinel — indicating the blob was written without encryption (legacy data or misconfiguration). This is an INTERNAL invariant violation: an encrypted-store checkpoint saver must never contain plaintext blobs once encryption is enabled. INTERNAL/Never — matches INTERNAL category default. Semantically distinct from E-CHKPT-004 (EncryptionKeyRotationFailed: the key exists but the rotation process failed) — E-CHKPT-007 means the blob was never encrypted in the first place. |
| E-CHKPT-008 | VAL | broken | BC-2.04.008 | `FtsLimitZero: FtsSearchConfig.limit must be > 0; got <limit>` (PC6/EC-004); also raised for malformed FTS5 query syntax: `FtsQuerySyntaxError: FTS5 query syntax error: <sqlite_error>` (EC-002) — both are VAL (caller-input) rejections. **(D20 sub-burst 2):** Split from original ambiguous single code in BC-2.04.008; FTS5-unavailable (deployment environment) moved to E-CHKPT-009. |
| E-CHKPT-009 | INTERNAL | broken | BC-2.04.008 | `Fts5Unavailable: FTS5 extension not available in this SQLite build — recompile SQLite with FTS5 support or use a pre-built distribution that includes it` (EC-006) — raised at `CheckpointSaver::new()` construction time when FTS is requested but `fts5_enabled()` returns false. INTERNAL/Never — environment/deployment misconfiguration, not a caller input error. **(D20 sub-burst 2):** Split from E-CHKPT-008 (VAL) because FTS5 unavailability is an INTERNAL invariant (wrong build) not a VAL caller error. |

### Component: SERVER (ferrochain-server)

> **Collision resolution note (sub-burst reconciliation 2026-07-13):** Batch 10 BCs
> authored codes 007–012 (Thread/Assistant/Run lifecycle errors). Batch 11 BCs independently
> numbered from 006. Resolution: Batch 10 codes 007–012 are authoritative; Batch 11 codes
> that collided (was 007/008/009) are renumbered to 013/014/015. Code 006 (ScheduleNotFound)
> had no collision and is preserved as-is.

| Error Code | Category | Severity | BC Anchor | RetryHint | Message Format |
|-----------|----------|----------|-----------|-----------|---------------|
| ~~E-SERVER-001~~ | ~~POLICY~~ | ~~broken~~ | ~~BC-2.13.003~~ | ~~Never~~ | ~~`PolicyNotEnforceable: ...`~~ — **RETIRED** (ADV-P1D-PASS-4 fix, 2026-07-14): duplicate of E-SBXD-002; mis-anchored to SERVER component when the error is emitted by ferrochain-sandbox. BC-2.13.003 emits E-SBXD-002; E-SERVER-001 was never used. Tombstone per append-only numbering policy. |
| E-SERVER-002 | VAL | broken | BC-2.12.003 | Never | `RunNotFound: run '<run_id>' does not exist in thread '<thread_id>'` |
| E-SERVER-003 | VAL | broken | BC-2.12.001 | Never | `ThreadNotFound: thread '<thread_id>' does not exist` |
| E-SERVER-004 | POLICY | broken | BC-2.12.005 | Never | `DebugRouteUnauthorized: debug/introspection route requires explicit opt-in configuration` — **Category correction (F-P25-02, ADV-P1D-PASS-25):** was AUTH → POLICY. See note below table. |
| ~~E-SERVER-005~~ | ~~POLICY~~ | ~~broken~~ | ~~BC-2.12.005~~ | ~~Never~~ | ~~`CorsRejected: CORS origin '<origin>' is not in the allow-list; default denies all cross-origin requests`~~ — **RETIRED** (ADV-P1D-PASS-66 fix, 2026-07-15): CORS denial is silent header-omission per BC-2.12.005 PC2/TV-001 — no error body is ever emitted; code retired unraised. Tombstone per append-only numbering policy. |
| E-SERVER-006 | VAL | broken | BC-2.12.004 | Never | `ScheduleNotFound: cron schedule '<cron_id>' does not exist` |
| E-SERVER-007 | CONCURRENCY | broken | BC-2.12.001 | Never | `ThreadAlreadyExists: thread '<id>' already exists` |
| E-SERVER-008 | POLICY | broken | BC-2.12.001 | Never | `ThreadStateConflict: thread '<thread_id>' has an active run '<run_id>'; state updates during active runs are disallowed` |
| E-SERVER-009 | VAL | broken | BC-2.12.002 | Never | `AssistantNotFound: assistant '<assistant_id>' does not exist` |
| E-SERVER-010 | VAL | broken | BC-2.12.002 | Never | `AssistantVersionNotFound: assistant '<assistant_id>' has no version <version>` |
| E-SERVER-011 | VAL | broken | BC-2.12.002 | Never | `GraphNotFound: graph '<graph_id>' is not registered with this server instance` |
| E-SERVER-012 | CONCURRENCY | broken | BC-2.12.003 | Never | `ConcurrentRun: thread '<thread_id>' already has an active run; use multitask_strategy to override` |
| E-SERVER-013 | VAL | broken | BC-2.12.005 | Never | `InvalidDebugRouteKey: debug_route_key must be non-empty` |
| E-SERVER-014 | DURABILITY | broken | BC-2.12.006 | Maybe | `RunStoreFailed: RunStore write failed for run '<run_id>' during transition '<transition>': <backend_error>` |
| E-SERVER-015 | CONCURRENCY | broken | BC-2.12.007 | Never | `RunAlreadyExecuting: run '<run_id>' is already being executed; concurrent execution rejected` |
| E-SERVER-016 | TIMEOUT | broken | BC-2.12.006 | Later | `IdempotencyLockTimeout: in-flight deduplication lock for key '<key>' held for ><timeout>s; lock_timeout is configurable via IdempotencyStore` |

> **E-SERVER-004 category correction (F-P25-02, ADV-P1D-PASS-25):** E-SERVER-004 was incorrectly
> categorized as AUTH (Category::Auth → HTTP 401). BC-2.12.005 PC4 + EC-002 mandate HTTP 403 and
> describe this as a policy gate (route inaccessible without explicit operator opt-in config), not a
> credential-based authentication failure. Recategorized AUTH → POLICY (Category::Policy → HTTP 403).
> The debug route is a capability gate, not an identity gate. The 401 row in interface-definitions.md
> §HTTP Status Codes carries no directly-emitted E-code in v1 (E-PROV-004 AUTH→401 is a categorical
> fallback surfaced embedded in Run.error, never a direct terminal response — see
> interface-definitions §HTTP Status Codes).

### Component: PROV (ferrochain-\<provider\>)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-PROV-001 | RATE | degraded | BC-2.08.004 | `RateLimited: provider '<provider>' returned 429; retry after <retry_after>s` |
| E-PROV-002 | TIMEOUT | broken | BC-2.08.007 | `ProviderTimeout: stream chunk timeout after <duration>` |
| E-PROV-003 | TRANSPORT | broken | BC-2.08.007 | `StreamInterrupted: TCP connection to '<provider>' reset mid-stream after <tokens> tokens` |
| E-PROV-004 | AUTH | broken | BC-2.08.004 | `ProviderAuthFailed: authentication failed` |
| E-PROV-005 | VAL | broken | BC-2.08.003 | `StructuredOutputParseError: <reason>` |
| E-PROV-006 | VAL | broken | BC-2.08.004 | `ContextLengthExceeded: provider '<provider>' rejected request — context length <actual> exceeds maximum <limit> tokens` |
| E-PROV-007 | POLICY | broken | BC-2.08.003 | `StructuredOutputRefused: <refusal_message>` — **(OBS-P28-3, ADV-P1D-PASS-28):** OpenAI Responses API `json_schema` safety-filter refusal path; POLICY because the model policy rejected the request, not a deserialization failure. RetryHint: Never (POLICY category default; refusal repeats for the same prompt). Surfaced embedded in Run.error in v1; categorical POLICY→403 fallback applies only if directly surfaced via HTTP. |
| E-PROV-008 | TRANSPORT | broken | BC-2.08.004 | `ProviderHttpError: provider returned HTTP <status>` (EC-004, non-JSON 5xx body); also `ProviderHttpError: unknown error format: <first 256 chars>` (EC-005, JSON body with unexpected schema) — **(ADV-P1D-PASS-56-COMPLETION, F-P78-SWEEP):** Generic provider HTTP 5xx and unparseable-error-body responses. Two distinct message forms per BC-2.08.004: (a) HTTP 5xx with non-JSON body (e.g., HTML error page): 'provider returned HTTP <status>'; (b) HTTP 4xx/5xx where the response body does not match the provider's documented error schema: 'unknown error format: <first 256 chars>'. Both forms carry TRANSPORT (caller cannot extract structured information). Semantically distinct from E-PROV-003 (StreamInterrupted) and E-PROV-002 (ProviderTimeout). RetryHint: Later (TRANSPORT default). Surfaced embedded in Run.error in v1; TRANSPORT→502 fallback applies only if directly surfaced via HTTP. |
| E-PROV-009 | VAL | broken | BC-2.08.013 | `ToolCallDialectParseError: <dialect> <element> payload is not valid JSON at response offset <n>: <parse_error>` (PC8 — HermesChatMlXml dialect; `<element>` = XML tag name e.g. `<tool_call>`); general form per PC9: dialect name + failure point — **(D20 sub-burst 2):** Raised when `ToolCallDialect::deserialize_tool_calls` returns `Err`. The content block was received from the provider but does not conform to the dialect's expected format (e.g., malformed Hermes ChatML XML). VAL — the content is caller-supplied (provider-returned) data that violates the dialect contract. RetryHint: Never (VAL default — the same dialect+content pair will always fail). |
| E-PROV-010 | POLICY | broken | BC-2.08.014 | `ProviderChainExhausted: all <N> providers in fallback chain failed; last error: <last_error_code>/<last_provider>` — **(D20 sub-burst 2):** Raised when `ProviderFallbackPolicy` has attempted every provider in the ordered chain and all returned retryable errors (429/5xx/auth). POLICY — the policy constraint (exhausted chain) rejects the operation; the caller must reconfigure the chain or fix the underlying provider issues. RetryHint: Never (POLICY default — adding more providers requires config change, not retry). |

### Component: MCP (ferrochain-mcp)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-MCP-001 | TOOL | broken | BC-2.09.004 | `ToolException: MCP server '<server>' raised ToolException for tool '<tool>': <message>` |
| E-MCP-002 | TRANSPORT | broken | BC-2.09.001 | `McpTransportError: cannot connect to MCP server '<server>': <transport_error>` |
| E-MCP-003 | VAL | broken | BC-2.09.001 | `McpNotImplemented: MCP server '<server>' does not implement '<method>'` — **(F-P66-01, ADV-P1D-PASS-66):** anchor corrected from BC-2.09.005 (connection-lifecycle; zero method-invocation surface) to BC-2.09.001 (list_tools discovery path — JSON-RPC -32601 MethodNotFound when server does not implement `tools/list`); EC-006/TV-008 added to BC-2.09.001. |
| E-MCP-004 | VAL | broken | BC-2.09.002 | `ToolNotFound: tool '<tool_name>' is not registered with any MCP server` |
| E-MCP-005 | TRANSPORT | broken | BC-2.09.006 | `McpServerBindFailed: cannot bind to <transport>: <reason>` (EC-001: SSE — EADDRINUSE; also EACCES for privileged port) — **(D20 sub-burst 2; RetryHint divergence: Never, overrides TRANSPORT default Later):** Socket bind failure requires operator configuration change (different port, address, or privilege grant). Retrying with the same bind configuration will immediately reproduce the same OS error. See RetryHint precedence blockquote for divergence rationale. |

### Component: SPLIT (ferrochain-splitters)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-SPLIT-001 | VAL | broken | BC-2.07.001 | `ZeroChunkSize: chunk_size must be > 0 code points; got 0` |
| E-SPLIT-002 | VAL | broken | BC-2.07.001 | `OverlapExceedsChunk: overlap <overlap> must be < chunk_size <chunk_size>` |

### Component: SBXD (ferrochain-sandbox)

| Error Code | Category | Severity | BC Anchor | Message Format |
|-----------|----------|----------|-----------|---------------|
| E-SBXD-001 | SECURITY | broken | BC-2.13.005 (also BC-2.13.004/VP-003 — shared canonicalize_beneath_root code path; VP-003 verifies the guard, BC-2.13.005 specifies the error surface) | `WorkspaceEscape: resolved path '<resolved>' escapes workspace root '<root>'` |
| E-SBXD-002 | POLICY | broken | BC-2.13.003 | `PolicyNotEnforceable: execution policy requires enforcing sandbox; non-enforcing backend is configured` |
| E-SBXD-003 | INTERNAL | broken | BC-2.13.001 | `SandboxInitFailed: cannot initialize WASM/container sandbox backend: <reason>` |
| E-SBXD-004 | POLICY | broken | BC-2.13.006 | `PlatformNoEnforcement: macOS Seatbelt allow-list cannot be enumerated for this tool — <reason>; use SandboxPolicy::allow_no_sandbox() to opt in to unsandboxed execution` |
| E-SBXD-005 | INTERNAL | broken | BC-2.13.006 | `BackendUnavailable: Seatbelt sandbox API is not supported on this macOS version — <reason>; does not silently fall back to process execution` |
| E-SBXD-006 | VAL | broken | BC-2.13.007 | `InvalidEnvAllowlistPattern: entry '<pattern>' contains wildcard characters — only exact variable names are supported in v1` — **(D20 sub-burst 2; F-P77-01 semantic correction):** Raised at `SandboxConfig` construction time when any entry in `env_allowlist` contains `*` or `?` (wildcard characters). VAL — `env_allowlist` accepts only exact variable names (case-sensitive, no wildcards); glob and regex patterns are not supported in v1. RetryHint: Never (VAL default — fix the entry to an exact name). Fail-closed: if any entry contains a wildcard, the entire allowlist is rejected and construction returns `Err`; no env-var is forwarded. |

### Component: RETRY (ferrochain-core retry combinator)

| Error Code | Category | Severity | BC Anchor | RetryHint | Message Format |
|-----------|----------|----------|-----------|-----------|---------------|
| E-RETRY-001 | POLICY | broken | BC-2.16.001 | Never | `RetryExhausted: per-tool retry limit for tool '<tool_name>' exhausted after <attempt_limit> attempts` |
| E-RETRY-002 | POLICY | broken | BC-2.16.002 | Never | `GlobalLimitExhausted: global retry budget of <global_limit> exhausted across all tools in this run` |
| E-RETRY-003 | POLICY | broken | BC-2.16.003 | `Later(<reset_timeout>)` | `CircuitBreakerOpen: tool '<tool_name>' circuit tripped after <failure_threshold> consecutive failures` |
| E-RETRY-004 | VAL | broken | BC-2.16.001 | Never | `InvalidRetryLimit: attempt_limit must be > 0; got <attempt_limit>` — **(F-P34-02, ADV-P1D-PASS-34):** BC-2.16.001 EC-003/TV-004 incorrectly referenced E-RETRY-003 (CircuitBreakerOpen, POLICY/Later) for construction-time zero-limit rejection. Minted as new code: VAL (misconfigured input, not a policy or circuit event), RetryHint Never (matches VAL default — not a divergence). |

### Component: CRON (ferrochain-server scheduler)

| Error Code | Category | Severity | BC Anchor | RetryHint | Message Format |
|-----------|----------|----------|-----------|-----------|---------------|
| E-CRON-001 | VAL | broken | BC-2.12.004 | Never | `AssistantNotFoundAtFiring: cron schedule '<cron_id>' fired but assistant '<assistant_id>' no longer exists` |
| E-CRON-002 | VAL | broken | BC-2.12.004 | Never | `InvalidCronExpression: field '<field>' value '<value>' is out of range — <reason>` |
| E-CRON-003 | POLICY | degraded | BC-2.12.004 | Later | `ScheduleQueueFull: cron schedule '<cron_id>' firing skipped; queue depth <queue_depth> exceeds max_queue_depth` |

### Component: MEMORY (ferrochain-memory)

| Error Code | Category | Severity | BC Anchor | RetryHint | Message Format |
|-----------|----------|----------|-----------|-----------|---------------|
| E-MEMORY-001 | VAL | broken | BC-2.15.001 | Never | `EmbeddingBackendNotConfigured: vector_search requires an embedding backend; none is configured` |
| E-MEMORY-002 | DURABILITY | broken | BC-2.15.001 | Never | `StorageFull: memory backend '<backend>' at '<path>' has no remaining capacity` |
| E-MEMORY-003 | POLICY | broken | BC-2.15.002 | Never | `ScopeAccessDenied: caller identity '<caller_identity>' cannot write to <requested_scope> — cross-owner lateral access denied` |
| E-MEMORY-004 | VAL | broken | BC-2.15.002 | Never | `NoScopeContext: memory scope resolution requires an active RunnableConfig session context; none is available` |
| E-MEMORY-005 | DURABILITY | broken | BC-2.15.003 | Never | `ErasurePartialFailure: GDPR erasure for user '<user_id>' partially completed; rolled back — <reason>` |
| E-MEMORY-006 | POLICY | broken | BC-2.15.003 | Never | `InsufficientPrivilege: operation '<operation>' requires <required>` — **(OBS-P106-A, 2026-07-18):** Corrected: was `requires AdminContext; caller has <caller_privilege>` — "AdminContext" was hardcoded where the BC parameterizes the `required` field; `<caller_privilege>` had no corresponding struct field (BC-2.15.003 EC-005 struct: `{ operation, required }`). BC wins on divergence (gate #33); taxonomy corrected to map 1:1 to struct fields. |
| E-MEMORY-007 | SECURITY | broken | BC-2.15.005 | Never | `MemoryWriteGuardDenied: write to namespace '<ns>' key '<key>' denied — <reason>` — **(D20 sub-burst 2):** Raised when `MemoryWriteGuard::validate` returns `WriteGuardDecision::Deny`. SECURITY — the guard prevents **prompt-injection and invisible-Unicode attacks via agent-controlled memory and skill writes**: the built-in scanner blocks role-injection prefixes (e.g., `"Human:"`, `"Assistant:"`, `"Ignore previous instructions"`), instruction-override markers, and invisible-Unicode characters (U+200B–U+200F, U+FEFF, U+202A–U+202E) that could corrupt agent memory with executable instructions — preventing an agent from poisoning its own skill or memory store. Authority: BC-2.15.005 EC-001 (role-injection), EC-002 (invisible Unicode), ADR-012 Decision 2 (new seam, separate from sandbox escape path). RetryHint: Never (SECURITY default — fix the malformed write content or the calling code). |
| E-MEMORY-008 | DURABILITY | broken | BC-2.15.004 | Maybe | `MemoryStoreReadFailed: backend read failed: <backend_error>` — **(F-P91-04, 2026-07-17):** Raised when a `MemoryStore` backend returns an I/O error on a read operation (e.g., SQLite file read failure in `load_skill`, `list_skills`, or `skill_exists`). DURABILITY — the storage read operation failed at the I/O layer. RetryHint: Maybe (DURABILITY default; transient I/O errors such as file lock contention may succeed on retry; no divergence from DURABILITY default). Semantically distinct from E-MEMORY-002 (StorageFull: write-capacity exhaustion) which is non-retryable. BC anchor: BC-2.15.004 EC-004 (load_skill I/O failure raise condition) + TV-008. |

### Component: BUDGET (ferrochain-graph budget governance subsystem)

> **Budget namespace reconciliation note (ADV-P1D-PASS-2 fix, 2026-07-14):** BCs BC-2.10.001–004
> were authored before a BUDGET component section existed, using `E-BUDGET-NNN` codes that had no
> taxonomy home. E-GRAPH-005 (BudgetCeiling) was the taxonomy's previous attempt to cover budget
> errors within the GRAPH component; it is now retired above. Budget governance errors use the
> BUDGET component (a subsystem of ferrochain-graph, following the RETRY/CRON pattern for
> intra-crate subsystems). Category decision: BudgetCeilingReached is POLICY (budget ceiling is a
> policy constraint that rejects the operation — matches POLICY definition verbatim); JournalWriteFailed
> is DURABILITY (storage I/O failure on the evidence journal write path).

| Error Code | Category | Severity | BC Anchor | RetryHint | Message Format |
|-----------|----------|----------|-----------|-----------|---------------|
| E-BUDGET-001 | POLICY | broken | BC-2.10.003 | Never | `BudgetCeilingReached: run halted: budget ceiling reached` |
| E-BUDGET-002 | DURABILITY | broken | BC-2.10.002 | Never | `JournalWriteFailed: budget evidence journal write failed for run '<run_id>': <backend_error>` |

## RFC-7807 Problem Emission

All `FerrochainError` values must be emittable as RFC-7807 problem+json when surfaced
via HTTP (ferrochain-server). The mapping is:

| FerrochainError field | RFC-7807 field |
|-----------------------|---------------|
| code (e.g., E-GRAPH-001) | `type` (URI: `urn:ferrochain:error:<code>`) |
| message | `detail` |
| category | `title` (humanized category name) |
| component | embedded in `type` URI |
| retry_hint | `extensions.retry_hint` (never/maybe/later) |

## RetryHint Values

| Value | Meaning | Client Behavior |
|-------|---------|----------------|
| `Never` | Retrying will not help; caller must fix input or config | Do not retry |
| `Maybe` | Retry once with same input; transient errors only | Retry once, then give up |
| `Later(Duration)` | Rate limit or temporary unavailability; wait and retry | Respect backoff |
