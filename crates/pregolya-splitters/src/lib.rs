#![forbid(unsafe_code)]
#![warn(missing_docs)]
//! pregolya-splitters — text splitting utilities.
//!
//! Modules (filled by TDD stories):
//! - `recursive` — `RecursiveSplitter` with Unicode code-point boundary counting (SS-07)
//! - `parity`    — golden-vector parity tests vs Python reference (R8)
