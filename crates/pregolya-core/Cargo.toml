[package]
name = "pregolya-core"
version.workspace = true
edition.workspace = true
description = "Core types, traits, and error taxonomy for pregolya"
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
# Outbound HTTP client with 30s timeout (BC-2.14.004); rustls-tls mandatory (CLAUDE.md)
reqwest = { workspace = true }

[dev-dependencies]
# Compile-time trait bound assertions (AC-005, AC-006 / VP-BC214001-02)
static_assertions = { workspace = true }
# anyhow compat test (BC-2.14.001 TV-004)
anyhow = { workspace = true }
# Compile-fail / compile-pass gate harness (AC-007 / BC-2.14.001 {PC-008})
# Validates #[non_exhaustive] external-boundary behaviour from outside the crate.
trybuild = { workspace = true }
# serde_json dev-only: ProblemDetail derives Serialize/Deserialize via serde only; serde_json needed solely for JSON assertion tests and the error_taxonomy_demo example (CR-001 provenance)
serde_json = { workspace = true }
# Async test runtime for BC-2.14.004 timeout integration test (AC-006 / S-1.02)
tokio = { workspace = true }

[lints]
workspace = true
