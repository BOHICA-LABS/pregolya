[package]
name = "pregolya-core"
version.workspace = true
edition.workspace = true
description = "Core types, traits, and error taxonomy for pregolya"
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true }

[dev-dependencies]
# Compile-time trait bound assertions (AC-005, AC-006 / VP-BC214001-02)
static_assertions = { workspace = true }
# anyhow compat test (BC-2.14.001 TV-004)
anyhow = { workspace = true }
# Compile-fail / compile-pass gate harness (AC-007 / BC-2.14.001 {PC-008})
# Validates #[non_exhaustive] external-boundary behaviour from outside the crate.
trybuild = { workspace = true }
# CLAUDE.md §reqwest TLS backend — JSON serialization in tests/examples only; serde_json not needed in production lib (CR-001 provenance)
serde_json = { workspace = true }

[lints]
workspace = true
